import React from 'react';
import '../pages/design.css';

const SignInButton = () => {
    return (
        <div className="sign-in-button">
            <button className="sign-in-text">Sign In</button>
        </div>
    );
};

export default SignInButton;